@model HomeViewModel
@using System.IO
@using Westwind.AspNetCore.Markdown
@{
    ViewData["Title"] = @Model.Title;
}

@if (@Program.config.EnableBreadcrumbs)
{
    @Html.Raw(@Model.breadcrumbs)
}


@Markdown.ParseHtmlStringFromFile(Model.Path)